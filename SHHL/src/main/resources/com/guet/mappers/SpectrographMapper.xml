<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guet.mapper.SpectrographMapper">
  <resultMap id="BaseResultMap" type="com.guet.entity.Spectrograph">
    <result column="TIME" jdbcType="TIMESTAMP" property="time" />
    <result column="states" jdbcType="INTEGER" property="states" />
    <result column="consistency" jdbcType="DECIMAL" property="consistency" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    TIME, states, consistency
  </sql>
  <select id="selectByExample" parameterType="com.guet.entity.SpectrographExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    'false' as QUERYID,
    <include refid="Base_Column_List" />
    from spectrograph_data
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <insert id="insert" parameterType="com.guet.entity.Spectrograph">
    insert into spectrograph_data (TIME, states, consistency
      )
    values (#{time,jdbcType=TIMESTAMP}, #{states,jdbcType=INTEGER}, #{consistency,jdbcType=DECIMAL}
      )
  </insert>
  <insert id="insertSelective" parameterType="com.guet.entity.Spectrograph">
    insert into spectrograph_data
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="time != null">
        TIME,
      </if>
      <if test="states != null">
        states,
      </if>
      <if test="consistency != null">
        consistency,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="time != null">
        #{time,jdbcType=TIMESTAMP},
      </if>
      <if test="states != null">
        #{states,jdbcType=INTEGER},
      </if>
      <if test="consistency != null">
        #{consistency,jdbcType=DECIMAL},
      </if>
    </trim>
  </insert>
</mapper>